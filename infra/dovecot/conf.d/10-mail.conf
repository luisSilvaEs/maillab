# infra/dovecot/conf.d/10-mail.conf

# Where mailboxes are stored on disk
# %u is substituted with the username at runtime
mail_location = maildir:/var/mail/vhosts/%u

# The system user Dovecot uses to access mailbox files
mail_uid = vmail
mail_gid = vmail